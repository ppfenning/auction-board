[pytest]
# The one pytest test shells out to `scala-cli test .`; -s lets the munit
# per-suite results it prints reach the console so `pytest -q` output is
# evidence of which Scala suites ran, not just that the wrapper passed.
addopts = -s -p no:cacheprovider
testpaths = tests
